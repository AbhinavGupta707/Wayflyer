# --- Core (needed for ETL + engine + mock API) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
duckdb>=0.10          # query layer / optional faster ETL
pandas>=2.2
pyarrow>=15.0         # parquet (optional cache format)
python-dotenv>=1.0
websockets>=12.0

# --- Agent loop (WS3) ---
langgraph>=0.2
langchain>=0.2
langchain-fireworks>=0.1
fireworks-ai>=0.15

# --- Voice (WS6) ---
elevenlabs>=1.0
twilio>=9.0

# NOTE: ETL + backtest run on the Python stdlib alone — `python -m keeper_api.engine.backtest`
# works with zero installs. Install the above for the API, agents, and voice.
